# 安全修复记录

本次检查覆盖 Express API、会话、数据库访问、Vue 中的数据展示和请求、依赖审计。没有修改既有数据库或车站数据，也没有执行线上渗透测试。

## 已修复

| 问题 | 修复 |
| --- | --- |
| `/confirm-delete` 可跳过前一步密码验证直接删除账户 | 最终删除接口自行验证密码；用户和车票在同一事务内删除；撤销该用户所有会话 |
| 修改账户信息无需旧密码，改密后旧会话仍然有效 | 前后端增加当前密码验证；更新后撤销全部旧会话，仅向当前客户端签发新会话；异步密码校验结束后再次核对账户状态 |
| 任意来源可获得携带凭据的 CORS 授权 | 改为明确的来源白名单，拒绝未授权 Origin，包括 `null` |
| 写操作缺少独立的 CSRF 防护 | 所有 API 写操作要求 `X-CRTicket-Request: 1`，前端统一发送；结合严格 CORS，阻止跨域脚本预检和普通表单伪造 |
| 图表提示框把车站名、日期拼接到 HTML | 所有图表提示框使用 ECharts `richText` 绘制，用户数据不再通过 HTML 提示框解析 |
| 认证请求无频率限制，客户端验证码可绕过 | 服务端认证接口每 IP 每 15 分钟最多 20 次；所有 API 每 IP 每分钟最多 300 次；使用异步 bcrypt，并让不存在用户的登录也执行哈希比较 |
| 非字符串、超长密码等输入会触发异常，bcrypt 会截断超过 72 字节的密码 | 服务端检查请求结构、用户名和密码类型/长度；拒绝超过 72 UTF-8 字节的密码；修改密码不再隐式去除空格 |
| 缺少车票字段与数据量限制 | 检查文本长度、数值类型和范围；请求体上限 1 MiB；备份单次最多 1000 条、每账户最多 10000 张；前端备份改为每批 100 张导入 |
| 会话缺少生产环境 Secure 标记，过期项不主动清理 | 生产环境 Cookie 启用 Secure，保留 HttpOnly/SameSite；定时清理；每账户最多 5 个会话、进程最多 10000 个 |
| API 出错可能返回 Express 默认错误详情，敏感响应可缓存 | 统一返回不含堆栈的 JSON 错误；设置 no-store、nosniff，禁用 X-Powered-By |
| 前端硬编码 HTTP localhost API，后端默认监听所有网卡 | 默认使用同源 `/api`，开发/预览通过 Vite 代理；后端仅监听 127.0.0.1:3000 |

数据库路径改为相对数据库模块解析，防止从不同目录启动时误用其他数据库；支持 `DATABASE_PATH` 供隔离测试或显式部署使用。启用 SQLite 外键约束。备份指纹改用 JSON 数组编码，避免字段中的分隔符造成错误去重。忽略生产环境配置和 SQLite 日志文件，减少意外提交的风险。

原有 SQL 使用参数绑定，车票操作在服务端按会话用户限制所有权；此次保留并通过回归测试验证，没有将它们误报为新修复的漏洞。

## 本地运行与生产部署

本地运行 `node server/app.js` 和 `npm run dev` 即可，默认后端端口仍为 3000。`npm run preview` 也配置了 API 代理。

生产环境必须使用 HTTPS 反向代理：静态服务目录仅设为 `dist/`，将 `/api` 转发到 `http://127.0.0.1:3000`，保持路径。不要将仓库根目录或 `server/db` 作为静态目录。启动后端之前设置：

```powershell
$env:NODE_ENV = "production"
$env:ALLOWED_ORIGINS = "https://你的实际域名"
node server/app.js
```

白名单填写完整 Origin（协议、域名、可选端口，不含路径或尾部斜杠），多个来源以逗号分隔；生产环境不自动信任 localhost。默认同源部署无需 `VITE_API_BASE_URL`；若使用独立 API 域名，在构建时设置它，并同时配置服务端白名单。Cookie 保留 SameSite=Lax，因此不支持不相关站点之间的跨站 Cookie 登录。

当前会话和限流状态仍存放于单进程内存，重启会注销全部用户并清空限流计数；多实例部署需要共享会话和限流存储。没有开启 Express `trust proxy`，防止客户端伪造转发头绕过限流；在反向代理后限额会按代理 IP 共享，应在实际网关上配置按客户端 IP 的限流，并只在明确可信的代理拓扑下调整代理信任设置。现有客户端验证码仅为交互功能，不作为安全边界。公网服务仍需网关限流应对分布式滥用。

历史上超过 72 字节的密码曾被 bcrypt 截断，此次会拒绝该类输入，需要由管理员提供可靠的密码重设途径，不应取消长度检查。已有超出新字段限制的数据没有被改写，但再次编辑时需要缩短。数据库备份、访问权限、TLS 证书和静态站点安全响应头仍由部署环境负责。

## 验证

- `node --test --test-isolation=none server/security.test.js`：使用内存 SQLite，覆盖恶意 Origin、缺失 CSRF 请求头、异常输入、请求体上限、认证、车票越权读写删除、注销绕过、改密会话撤销、Cookie 属性、限流及合法操作。
- `npm run build`：生产构建通过；仅有依赖注解及打包体积警告。
- `npm audit --json`：此次检查报告 0 个已知漏洞；这不等于依赖绝无漏洞，也不覆盖未收录的漏洞。
- 图表 XSS 修复经过源码检查和编译验证，未运行浏览器端攻击载荷测试。

设计参考：[OWASP CSRF 防护](https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html)、[OWASP 认证安全](https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html)。
